# jar-shell specific configuration
# Extends: application-contract-base.yml
marketdata:
  url: http://localhost:10991/api

position:
  url: http://localhost:10993/api

ratesIgnored: true
auth:
  enabled: false
spring:
  shell:
    interactive:
      enabled: false
    script:
      enabled: false
  security:
    oauth2:
      registration:
        custom:
          client-id: not-set
          client-secret: not-set
