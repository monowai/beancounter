marketdata:
  url: http://localhost:10999
ratesIgnored: true
auth:
  enabled: false
spring:
  shell:
    interactive:
      enabled: false
    script:
      enabled: false
  security:
    oauth2:
      registration:
        custom:
          client-id: not-set
          client-secret: not-set
