package com.beancounter.common.model

/**
 * Available grouping options for displaying holdings.
 * Determines how positions are grouped in the holdings view.
 */
enum class GroupByPreference {
    ASSET_CLASS,
    SECTOR,
    MARKET_CURRENCY,
    MARKET
}