kafka:
  enabled: "true"
  broker.port: 0

spring:
  kafka:
    consumer:
      group-id: data-test
  embedded:
    kafka:
      brokers: "localhost:${kafka.broker.port}"


beancounter:
  market:
    providers:
      FIGI:
        enabled: false

  topics:
    ca:
      event: topicEvent