server:
  port: 0

kafka:
  enabled: false

beancounter:
  # All Asset enrichment via OpenFigi
  enricher: FIGI
  marketdata:
    provider:
      FIGI:
        url: http://localhost:6666
        key: demoxx
